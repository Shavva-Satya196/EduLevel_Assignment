
## Run Locally

**Prerequisites:**  Node.js
1.git clone //link

2. Install dependencies:
   `npm install`
3. Set the `GEMINI_API_KEY` in [.env.local](.env.local) to your Gemini API key
   Get the API key from https://aistudio.google.com/api-keys
4. Run the app:
   `npm run dev`
